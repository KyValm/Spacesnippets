version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Space-snippets/Space-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
